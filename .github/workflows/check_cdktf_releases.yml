name: Build and Push docker image

on:
  release:
    types: [published]

jobs:
  check_cdktf_releases:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build
      run: make build

    - name: Test
      run: make run